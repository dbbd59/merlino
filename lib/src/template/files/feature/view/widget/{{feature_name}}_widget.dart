import 'package:flutter/material.dart';

class {{feature_name_capitalize}}Widget extends StatelessWidget {
  const {{feature_name_capitalize}}Widget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      '{{feature_name}}',
    );
  }
}
